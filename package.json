{
  "name": "erp-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web"
  },
  "dependencies": {
    "jspdf": "^4.2.1",
    "xlsx": "^0.18.5"
  }
}
